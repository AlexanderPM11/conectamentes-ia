import { StarDisplay } from './StarDisplay';

export function ReputationSummary({ reputation, title, compact = false }: { reputation: any; title: string; compact?: boolean }) {
  if (!reputation?.total) return <section className={`reputation-summary${compact ? ' compact' : ''}`}><div><p className="eyebrow">REPUTACIÓN</p><h2>{title}</h2><p className="reputation-empty">Tus calificaciones aparecerán aquí después de impartir una orientación completada.</p></div></section>;
  return <section className={`reputation-summary${compact ? ' compact' : ''}`}><div className="reputation-score"><span>{Number(reputation.average).toFixed(1)}</span><StarDisplay value={reputation.average} /><small>{reputation.total} {reputation.total === 1 ? 'valoración' : 'valoraciones'}</small></div><div className="reputation-body"><p className="eyebrow">REPUTACIÓN</p><h2>{title}</h2><div className="dimension-grid"><span><b>{Number(reputation.clarity).toFixed(1)}</b>Claridad</span><span><b>{Number(reputation.usefulness).toFixed(1)}</b>Utilidad</span><span><b>{Number(reputation.fulfillment).toFixed(1)}</b>Cumplimiento</span><span><b>{Number(reputation.respect).toFixed(1)}</b>Respeto</span></div>{reputation.topics?.length > 0 && <div className="topic-ratings">{reputation.topics.map((item: any) => <span key={item.topic}><b>{item.topic}</b><StarDisplay value={item.average} /><small>{Number(item.average).toFixed(1)} · {item.total}</small></span>)}</div>}{reputation.comments?.length > 0 && <div className="rating-comments">{reputation.comments.slice(0, compact ? 6 : 3).map((item: any, index: number) => <blockquote key={`${item.createdAt}-${index}`}><p>“{item.comment}”</p><footer>{item.topic}</footer></blockquote>)}</div>}</div></section>;
}
